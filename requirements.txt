# ComfyUI Uncensored Desktop — runtime dependencies
# NOTE: The shipping application is built with CustomTkinter (see ComfyUI_App.py).
# The PyQt6 modules under spark_plan_qt6/ are a PROPOSED alternative architecture
# and are NOT imported by the running app. They are kept here only as a reference
# snapshot of the Spark Improvement Plan and are not required to run the app.

# --- Required by the shipping CustomTkinter app ---
customtkinter>=5.2
pillow>=10.0
psutil>=5.9
websocket-client>=1.6
httpx>=0.27
requests>=2.31

# --- Optional hardware telemetry (auto-detected at runtime) ---
# torch          # only if a local CUDA backend is managed directly
# pynvml         # NVIDIA VRAM telemetry (falls back to torch.cuda when present)

# --- Proposed PyQt6 architecture (spark_plan_qt6/, NOT used at runtime) ---
# PyQt6
# PyQt6-WebEngine
# PyQt6-WebChannel
