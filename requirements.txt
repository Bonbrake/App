# ComfyUI Uncensored — runtime dependencies
# Build/run with Python 3.11 (the frozen EXE is built against 3.11).
customtkinter>=5.2
tk
Pillow>=10.0
requests>=2.31
numpy>=1.24
imageio>=2.31
imageio-ffmpeg>=0.4
pywin32>=306
