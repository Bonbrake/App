# ComfyUI Uncensored Desktop — LOCKED runtime dependencies
#
# Exact versions used to build and verify the shipped release.
# Reproduce the build environment with:
#
#     py -3.11 -m venv .venv
#     .venv\Scripts\python -m pip install -r requirements.lock.txt
#     py -3.11 build_exe.py
#
# Interpreter: CPython 3.11.9 (64-bit, Windows)
# Build tool:  PyInstaller 6.21.0
#
# Regenerate deliberately, not casually — these pins are the difference
# between a reproducible bundle and "works on my machine".

# --- Required: imported unconditionally by the shipping app ---
customtkinter==6.0.0
pillow==12.3.0
requests==2.34.2
numpy==2.4.6

# --- Required for optional-but-shipped features (guarded imports) ---
psutil==7.2.2
imageio==2.37.4
imageio-ffmpeg==0.6.0
pywin32==312

# --- Transitive/support pins observed in the verified build env ---
darkdetect==0.8.0
packaging==26.2

# --- Build-time only ---
pyinstaller==6.21.0
